language: python
python:
  - "2.7"

install: 
  - pip install -r requirements.txt
  - pip install .
script:
  - cedula -n V -r 10000000-10000100 -s 5 --db-file cedula.db
  - cedula -n V -r 10000000-10000010 --show --db-file cedula.db
